# Directus Railway Template - Environment Variables
# Railway will auto-detect these and pre-fill with defaults
# You only need to set SECRET - everything else has sensible defaults

# =============================================================================
# REQUIRED - Only SECRET needs to be set (others have defaults)
# =============================================================================

# Secret key for encryption (32+ characters) - YOU MUST SET THIS
# Generate with: openssl rand -hex 32
# Or use Railway's secret generator in the Variables UI
SECRET=

# Admin user credentials (defaults provided, but recommended to change)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=d1r3ctu5

# Public URL (Railway will auto-fill this, but you can customize)
PUBLIC_URL=https://${{RAILWAY_PUBLIC_DOMAIN}}

# =============================================================================
# OPTIONAL - Pre-filled with defaults, change if needed
# =============================================================================

# Template to load (defaults to "cms" - includes pre-configured collections)
# Set to empty string for blank instance
DIRECTUS_TEMPLATE=cms
